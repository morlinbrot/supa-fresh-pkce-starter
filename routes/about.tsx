import { PageHeader } from "$components/PageHeader.tsx";

export default function Protected() {
  return <PageHeader title="About Page" subtitle="You have reached the About Page which is visible to anyone." />;
}
